{
  "name": "Visit",
  "type": "object",
  "properties": {
    "family_id": {
      "type": "string",
      "title": "Family ID"
    },
    "volunteer_id": {
      "type": "string",
      "title": "Volunteer ID"
    },
    "volunteer_name": {
      "type": "string",
      "title": "Volunteer Name"
    },
    "date": {
      "type": "string",
      "title": "Date",
      "format": "date"
    },
    "visit_type": {
      "type": "string",
      "title": "Visit Type"
    },
    "note": {
      "type": "string",
      "title": "Note"
    }
  },
  "required": [
    "family_id",
    "volunteer_id",
    "volunteer_name",
    "date",
    "visit_type"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "$and": [
            {
              "created_by": "{{user.email}}"
            },
            {
              "data.volunteer_id": "{{user.data.my_volunteer_id}}"
            },
            {
              "data.family_id": {
                "$in": "{{user.data.my_committed_family_ids}}"
              }
            }
          ]
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "created_by": "{{user.email}}"
        },
        {
          "data.volunteer_id": "{{user.data.my_volunteer_id}}"
        },
        {
          "data.family_id": "{{user.data.my_family_id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "created_by": "{{user.email}}"
        },
        {
          "data.volunteer_id": "{{user.data.my_volunteer_id}}"
        },
        {
          "data.family_id": "{{user.data.my_family_id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "created_by": "{{user.email}}"
        },
        {
          "data.volunteer_id": "{{user.data.my_volunteer_id}}"
        },
        {
          "data.family_id": "{{user.data.my_family_id}}"
        }
      ]
    }
  }
}